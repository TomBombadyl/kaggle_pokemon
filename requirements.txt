# Core deps (re-installed each run; sandbox is fresh nightly).
kaggle>=1.7.4          # 1.7+ required; 1.6.x only supports legacy kaggle.json
kagglehub>=1.0         # supports the new KAGGLE_API_TOKEN (KGAT_) bearer token
numpy>=1.26
pandas>=2.2
tqdm>=4.66
pyyaml>=6.0
# Ladder/eval harness — installs only on Python >= 3.11 (see PROGRESS.md):
#   kaggle-environments==1.30.1
# The `cabt` env + `cg/` engine ship from the Kaggle competition page, not PyPI.
